export interface Product {
    id: string;
    productName: string;
    productCategory: string;
    productDesc: string;
    productImg: string;
    location: string;
    productPrice: number;
    bookingDate: string; // Change to a Date type if needed
    startTime: string; // Change to a Date type if needed
    endTime: string; // Change to a Date type if needed
  }
  